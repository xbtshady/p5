/**
 * 配置模板（入库）
 *
 * 使用方法：复制本文件为 config.js，填入你的 CloudBase 环境 ID。
 * config.js 已被 .gitignore 忽略，不会提交到仓库。
 */
window.APP_CONFIG = {
  // 必填：CloudBase 环境 ID，在云开发控制台「环境 -> 环境概览」可查
  envId: "your-env-id",

  // 可选：环境所在地域，默认上海。必须与环境实际地域一致
  region: "ap-shanghai"
};
